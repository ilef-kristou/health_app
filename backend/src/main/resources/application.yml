server:
  port: 9001               # change si tu veux

spring:
  application:
    name: mini-test

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    prometheus:
      enabled: true
    health:
      show-details: always